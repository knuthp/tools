{
  "name": "tools-collection",
  "version": "1.0.0",
  "description": "Mischellaneous HTML+JavaScript or python script tools",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "build": "bash scripts/cf_publish.sh",
    "deploy": "npm run build && wrangler pages deploy",
    "preview": "npm run build && wrangler pages dev"
  },
  "devDependencies": {
    "eslint": "^9.39.4",
    "eslint-plugin-html": "^8.1.4",
    "globals": "^17.4.0",
    "wrangler": "^4.76.0"
  }
}
